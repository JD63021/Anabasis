# SIMPLE reference cases

Use these with the steady SIMPLE app/executable:

```bash
./simple_gpu_dp -case-config cases/simple/cylinder_re20_steady_simple.case
```

`nsteps` means SIMPLE outer iterations in these case files.
